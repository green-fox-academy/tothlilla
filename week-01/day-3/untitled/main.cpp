
int  main (doublearray (int(&examplearray)[])){
    int size = 5;
    int newarray[size];
    for (int i = 0; i < size; ++i) {
        newarray[i]= 2 * examplearray[i];
    }
    return newarray[size];

}